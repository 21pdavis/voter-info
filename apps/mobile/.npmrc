# Expo + Clerk pull in transitive React DOM peer ranges that npm's strict
# resolver rejects against the SDK-pinned react version. Expo's own
# `expo install` already needs this; make it the default for the workspace.
legacy-peer-deps=true
