name: CI

on:
  push:
    branches: [main, qa]
  pull_request:

jobs:
  api:
    name: API typecheck & build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install
      - run: npm run build

  mobile:
    name: Mobile typecheck
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install
      - run: npx tsc --noEmit

  deploy:
    name: Deploy to Render
    needs: [api, mobile]
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/qa'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger API deploy hook (prod)
        if: github.ref == 'refs/heads/main'
        run: |
          if [ -z "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" ]; then
            echo "RENDER_DEPLOY_HOOK_URL secret not set — skipping deploy."
            exit 0
          fi
          curl -fsSL "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
      - name: Trigger API deploy hook (qa)
        if: github.ref == 'refs/heads/qa'
        run: |
          if [ -z "${{ secrets.RENDER_QA_DEPLOY_HOOK_URL }}" ]; then
            echo "RENDER_QA_DEPLOY_HOOK_URL secret not set — skipping deploy."
            exit 0
          fi
          curl -fsSL "${{ secrets.RENDER_QA_DEPLOY_HOOK_URL }}"
      - name: Trigger web deploy hook (prod)
        if: github.ref == 'refs/heads/main'
        run: |
          if [ -z "${{ secrets.RENDER_WEB_DEPLOY_HOOK_URL }}" ]; then
            echo "RENDER_WEB_DEPLOY_HOOK_URL secret not set — skipping deploy."
            exit 0
          fi
          curl -fsSL "${{ secrets.RENDER_WEB_DEPLOY_HOOK_URL }}"
      - name: Trigger web deploy hook (qa)
        if: github.ref == 'refs/heads/qa'
        run: |
          if [ -z "${{ secrets.RENDER_WEB_QA_DEPLOY_HOOK_URL }}" ]; then
            echo "RENDER_WEB_QA_DEPLOY_HOOK_URL secret not set — skipping deploy."
            exit 0
          fi
          curl -fsSL "${{ secrets.RENDER_WEB_QA_DEPLOY_HOOK_URL }}"
